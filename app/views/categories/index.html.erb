<h1>Listing categories</h1>

<table>
  <thead>
    <tr>
      <th>Kategoria:</th>
      <% if user_signed_in? %><th>Opcje:</th><% end %>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= link_to category.name, category %></td>
        <% if user_signed_in? %><td><%= link_to 'Edytuj', edit_category_path(category) %>
        <%= link_to 'Usuń', category, method: :delete, data: { confirm: 'Na pewno?' } %></td><% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @categories %>

<br>

<%= link_to 'New Category', new_category_path %>
